{% set mytk = os.getenv('CFI_TOKEN') %}
{
    "credentials": [
        {
            "url": "https://superfrogchrismc.jfrog.io/artifactory/api/generic/cfi-backup-sources",
            "token": "{{mytk}}"
        }
    ]
}
