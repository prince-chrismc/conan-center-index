{% set mytk = os.getenv('CFI_TOKEN') %}
{
    "credentials": [
        {
            "url": "https://superfrogchrismc.jfrog.io/artifactory/cfi-backup-sources/",
            "token": "{{mytk}}"
        }
    ]
}
